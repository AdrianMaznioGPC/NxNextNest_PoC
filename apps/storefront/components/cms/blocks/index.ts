// Import all blocks so they self-register with the block registry.
// To add a new block: create the component file and add an import here.
import "./hero-banner";
import "./featured-products";
import "./product-carousel";
import "./rich-text";
